{
	"name": "crapnote-extension",
	"private": true,
	"version": "0.1.0",
	"description": "CrapNote browser extension (Chrome/Brave + Firefox)",
	"type": "module",
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"check": "tsc --noEmit",
		"build": "node build.mjs",
		"build:chrome": "node build.mjs chrome",
		"build:firefox": "node build.mjs firefox"
	},
	"devDependencies": {
		"@types/chrome": "^0.0.280",
		"esbuild": "^0.28.2",
		"happy-dom": "^20.11.12",
		"typescript": "^5.6.0",
		"vitest": "^4.1.11"
	}
}
